classdef OptimizationResultSummary < handle
   
    % This POD contains some output information as to how well the
    % optimization succeeded
    
    properties(Access = public)
       
        % Actual number of iterations used
        iterations;
        
    end
    
end